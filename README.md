## Start Running the Project Locally

Once the project has been cloned, first install the dependencies with:

```bash
npm install
```

## Then, to run the local development server, execute:

```bash
npm start
```
